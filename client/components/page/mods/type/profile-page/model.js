// import API from 'lib/api';
import Model from 'lib/component-model';
// import EVENTS from 'lib/events';

// const GATE = '/api/components/Profile';

export default class ProfilePageModel extends Model {
  constructor(view) {
    super(view);
  }
}
